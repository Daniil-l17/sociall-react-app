import styled from '@emotion/styled';

export const $LayoutWrapper = styled.div({
	maxWidth: '1350px',
	margin: '0 auto',
	padding: '0px 15px'
});

export const $LayoutWrapperContext = styled.div({
	display: 'flex',
	marginTop: '0',
	paddingTop: '40px'
});

export const $LayoutContentFlex = styled.div({
	flex: 1,
	display: 'flex'
});
